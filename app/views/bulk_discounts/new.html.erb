<%# <%= render partial: "shared/nav" %> 

<p> Note: fill in Percentage discount as a decimal i.e. 0.2 = 20% </p>
<%= render partial: 'form', locals: { method: :post, url: merchant_bulk_discounts_path(@merchant, @bulk_discount), class: 'bulk_discounts_form', local: true, model: nil } %>

