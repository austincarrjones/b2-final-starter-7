<%# <%= render partial: "shared/nav" %> 

<%# <%= render partial: 'form', locals: { method: :post, url: merchant_bulk_discount_path(@merchant, @bulk_discount), class: 'bulk_discounts_form', local: true, model: nil } %> %>

